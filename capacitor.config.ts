import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jxk.virtualized',
  appName: 'rhooks-virtualized',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
